# AGENTS — The Hokuten Group Platform

Working rules for every agent (Claude Code, Cursor, or human) in this workspace. `CLAUDE.md` is a symlink to this file — one rulebook, every tool.

## Read these first, in order

1. This file.
2. [PROJECT-MEMORY.md](PROJECT-MEMORY.md) — standing decisions + dated log. Never re-litigate a decision silently; propose changes as new dated entries.
3. The skill relevant to your task (`.agents/skills/`): design/UI/brand/copy → `hokuten-design-director` (load only the reference files its verbs tell you to).
4. [docs/PHASE-1-IMPLEMENTATION.md](docs/PHASE-1-IMPLEMENTATION.md) — scope-of-record for what the site *is*.
5. [docs/LAUNCH-IMPLEMENTATION.md](docs/LAUNCH-IMPLEMENTATION.md) (`approved`, 2026-08-17) — the launch delta on top of it: Dino's Brand Design Guide v1.3 retune, his locked content, and launch config. [BRAINSTORM.md](BRAINSTORM.md) is ideas, not scope.

## Workspace map

- `site/` — the Next.js app (created in Phase 1, M0). All product code lives here.
- `.agents/skills/` — canonical skills; `.claude/skills` and `.cursor/skills` are symlinks to it. Edit skills only in `.agents/skills/`.
- `.cursor/rules/*.mdc` — thin enforcement pointers; the substance lives in skills/docs. Keep them in sync when tokens or rules change.
- `The_Hokuten_Group_Brand_Addon_2/` — brand masters, tracked and read-only (10 lockups + 6 covers + `README.txt`); export copies into `site/public/brand/`.
- `full-brand-toolkit/` — Dino's full 2026-08-17 delivery (agreements, manuals, kits). **Gitignored, never committed.** Read it in place; the build's source assets are copied out into tracked `Ref/` subfolders instead. (R17, 2026-08-17.)
- `Ref/` — hand-picked design references and **build-time source assets**: `Ref/awards/` (the five approved CoStar Winner Badges), `Ref/team/` (canonical uniform headshots), `Ref/brand-kit/` (the kit lockup PNGs the prep scripts read), `Ref/listings/`, `Ref/artwork/`, `Ref/hotel-brands/`, `Ref/site/`. Prep scripts read `Ref/` at build time; **no runtime path may ever begin with `Ref/`** (D21, ref 07 P0 gate) — components read only prepared files under `site/public/`. (Subfolder set added 2026-08-17, R17.)
- `chat-context.md` — private team chat. Never commit, quote in public artifacts, or copy credentials from it.
- Port source: `~/Documents/Dino/dino-sites/kwc-dinomonteverde/` (read-only — never edit Dino's live-site repo from here).

## Working rules

- **Memory protocol** (non-negotiable, from the expo/razim-co convention): before every push — and after every decision, scope change, or shipped section — add a dated entry to PROJECT-MEMORY.md. Newest first. Convert relative dates to absolute.
- **Evidence gate**: no public claim (stat, award, testimonial, logo) ships without a `verified-current` row in the design skill's claims register (reference 06).
- **Decision protocol**: pre-resolve decisions in plans before implementing; a plan with open questions is not ready to execute.
- **Status vocabulary** everywhere: `provisional | approved | blocked: <reason>` for specs; `P0/P1/P2` for findings; `parked | exploring | approved | building | shipped | rejected` for ideas.
- **References are translated, never cloned.** New inspiration goes through the `study` verb into reference 02.
- **Trendy-library quarantine**: liquid-dom, threecn, and similar are study-only until a compatibility/a11y/perf spike passes and is logged.
- **Verify APIs against `node_modules` docs, not memory** — especially Next.js and Tailwind v4.

## Hard guardrails — do not change without Razim's explicit OK

- Spelling: HOKUTEN. Brand: THE HOKUTEN GROUP. Never "Hakuten" in any artifact.
- Website gold `#B08D3F` (`--accent`); gold-dim `#C8A552` (`--accent-dim`); kit/raster gold `#B8943D` only inside raster assets. Hex values live in skill reference 01 and `site/app/globals.css` only (plus the generator scripts that bake rasters). **(L2 / R3, Razim 2026-08-17 — supersedes ~~"Website gold `#B8902E`; kit gold `#B8943D` only inside raster assets"~~ from 2026-08-07. Sampled evidence: all eight kit lockup PNGs in `The_Hokuten_Group_Brand_Addon_2/01_Logo_Lockups/` bake `#B8943D`; Dino's Brand Design Guide v1.3 line 19 and the Hokuten Work Manual §13 line 679 both independently print Hokuten Gold `#B08D3F`. The CSS takes the guide value; rasters keep their baked `#B8943D`, so a ~2-unit hue/value delta sits beside a `#B08D3F` rule and is accepted, not hidden (D14 — Dino eyeballs it on the production deployment). Full text: docs/LAUNCH-IMPLEMENTATION.md §1 L2, §8 R3, §9 X4.)**
- Typography is three voices, no more: **Cormorant Garamond** (display) · **Inter** (UI/body) · **JetBrains Mono** (data + micro-labels), self-hosted via `next/font`, ≤2 files per family. **(L3 / R13, Razim 2026-08-17 — supersedes ~~"Fraunces (display) / Inter / IBM Plex Mono (data)"~~, approved 2026-08-10 in Design Revisit 2 and restated as a design-skill non-negotiable in `.agents/skills/hokuten-design-director/SKILL.md`; both carry the dated supersession. Source: Brand Design Guide v1.3 lines 10–16, corroborated by the kwc port source, which declares the same three faces. Without this the design skill's own `audit` verb flags the shipped faces P0.)**
- **Theme G is the production theme. Theme B is parked, not deleted** — its `[data-theme="blue"]` block, `THEME_PRESENTATION.blue` and every `IS_BLUE` branch stay in the tree, unreachable and deliberately **not** retuned to the v1.3 palette; `DEFAULT_THEME` stays `"gold"`, no user-facing switch is added, and the `theme-blue` branch stops being fast-forwarded. **(L1 / R12, Razim 2026-08-17 — supersedes ~~"Phase 1 ships TWO complete theme variants… as two Vercel preview links for team comparison"~~ from 2026-08-07 for everything downstream of production. Parked, not repealed: the code stays so the decision is reversible, which is also why Theme B's blue values are still the old ramp and must not be read as current brand law.)**
- **Listings render from `site/content/listings.ts`** — the rendered source of truth — under a three-property allowlist (The Florida Gateway · Quality Suites Houston NW Cy-Fair · Pocono Mountain Hotel & Spa). `/api/public-listings` is an **additive** same-origin server-side proxy of the a100 feed with the same three-ID allowlist and a public-field whitelist: a record outside the allowlist is dropped, logged server-side and never rendered, and browser code never points at the a100 endpoint. **(L8, Razim 2026-08-17 — supersedes ~~"Phase 1 listings/closed/content = static typed modules… Later phases integrate the a100arms.com API"~~ from 2026-08-07. What changed is that a feed exists at all; what did not change is that the static module is what renders.)**
- **Awards copy is 4 + 1, never merged, never counted.** Four dated INDIVIDUAL CoStar awards (2025 Annual Top Broker; Quarterly Deals Q3 2025, Q1 2026, Q2 2026) in one strip, then the **2025 Annual Top Firm** in its own separately-labelled prior-firm/team block. Never "5×"/"five-time", never "Annual 2026", never "12 closed hotel sales", never "hotel investment platform", and never framed as Hokuten team recognition (Hokuten did not exist in 2025). Badge artwork ships unmodified — no crop, recolor, redraw or combine. **(L6, Razim 2026-08-17; source `V2` §2/§3 + the CoStar README. New guardrail, supersedes nothing.)**
- Header brand mark: the theme-matched KW/Hokuten lockup (blue lockup on Theme B, gold on Theme G) **is** the header mark — a real-text brand line stays adjacent (visually-hidden is acceptable) for SEO/AT; names baked only into an image, with no real-text fallback, is the Sarhan-era anti-pattern to avoid. Footer keeps exactly ONE KW Commercial compliance mark instance (the 2026-08-08 audit found it rendering twice — the duplicate is a bug, not a feature) plus the verbatim disclosure line; compliance/TCPA blocks stay byte-exact ports. **(D1, Razim 2026-08-08 — supersedes "Hokuten-first branding; KW Commercial only as footer compliance mark + verbatim disclosure line" from 2026-08-07; the old "no KW lockup in the header" P0 is retired. Full text: docs/DESIGN-REVISIT.md §2.)** **Extended 2026-08-17 (L4 / R14):** on production (Theme G) the header mark is the **linear** cut, prepared from `KW_Commercial_Linear_TheHokutenGroup_Gold_on_Charcoal.png`, on the site's existing **dark** nav bar. That is a **recorded deviation** from Dino's master directive `V2` §1 line 8, which orders a *white* nav bar because the linear lockup carries dark lettering — the on-charcoal cut removes the legibility reason for the white bar without inverting the chassis. Recorded, not hidden: Dino reviews it on the production URL. The stacked badge stays the client-facing mark everywhere else (menu overlay, `#stats` identity anchor, footer, OG); the favicon is the hanko glyph, never a lockup.
- Calculator math, defaults, and cap-rate `CONFIG` are a frozen port from the kwc site — changes require a dated PROJECT-MEMORY.md decision.
- No Sarhan Hotel Group branding anywhere on the new site.
- Secrets: `FRED_API_KEY` is a Vercel env var, server-side only. Never commit `.env*`.
- Vercel: deploys go through GitHub → Vercel integration (auto-deploy on push; never CLI-deploy). Local CLI (authed as `razim-kw`) is allowed ONLY for link/env/branch config on team `hokuten1`, project `hokuten` — run `vercel whoami` first and pass `--scope hokuten1` on every command. Dino's `dino-kwc` and the a100arms projects are on other teams: off-limits from this machine.
- Do not deploy publicly under the Hokuten name until the KW / Forward Wilshire paperwork gate clears (tracked in PROJECT-MEMORY.md open items). **Clarified 2026-08-17 (L15) — the guardrail is not lifted, its boundary is named:** pushing to the Vercel **production deployment** while it still emits `noindex, nofollow` is allowed and is how Dino reviews. **The cutover is what stays gated** — removing `noindex` (both mechanisms: `lib/seo.ts` `INDEXING_ENABLED` *and* the hardcoded `robots` object in `app/layout.tsx`), pointing `thehokutengroup.com`, and the 301 from `kwc-dinomonteverde.com` — until the FBN filing, the broker approval email and the licence-number gates are all evidenced.
- git: repo root is this folder; remote is `https://github.com/rawzm/hokuten.git` (branch `main`); `chat-context.md` stays gitignored. Commits authored as rawzm. **No Co-Authored-By or any AI/co-author attribution trailers in commit messages — ever** (Razim, 2026-08-07).

## Definition of done (any user-visible work)

Spec `approved` → built to tokens → passes the design skill's `audit` (no P0) → perf gates green (reference 05) → QA greps pass (reference 07) → PROJECT-MEMORY.md entry written.
