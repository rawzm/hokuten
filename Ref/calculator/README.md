# Calculator property-type reference images — drop crops here

DESIGN-REVISIT-2.md §4.3. One per property type, named for the frozen key in
`site/lib/valuation.ts`:

    limitedService.jpg  selectService.jpg  fullService.jpg
    resortBoutique.jpg  extendedStay.jpg

3:2 — 1600x1067 ideal, 1200x800 minimum.

Approved artwork is reused via `site/content/artwork.ts` until dedicated crops
land here. `extendedStay` is the known gap. Never substitute stock.
Production reads only `site/public/` output.
