# Audit Log

Append-only. Newest entries at the bottom. Never delete or overwrite a prior entry.

---

**Artifact:** Token + Dual-Theme gate — every file under `site/components/` and `site/app/` (mechanical hunt: raw hex/`rgb()`/`hsl()`, Tailwind default-palette colours, direct `--gold`/blue-hex references, opacity-faked text colour, `star-grain`/`plate-frame` surface scoping, sections hardcoding a surface colour instead of a `.surface-*` scope).
**Date:** 2026-08-08

**P0 (blocker):** `SiteFooter.tsx`'s brand cluster hardcodes the Theme-G-only raster lockup with no theme branch and no Theme B asset to branch to, breaking the site on `theme-blue` — gate: "Any direct `--gold` reference or a blue hex in a component — P0, it breaks the other theme" (`docs/AGENT-BRIEF.md` Token law).
  - Evidence: `site/components/sections/SiteFooter.tsx:108-114` —
    ```tsx
    <Image
      src="/brand/lockup-stacked-gold.png"
      alt=""
      width={240}
      height={184}
      className="h-10 w-auto sm:h-12"
    />
    ```
    literal, unconditional `"/brand/lockup-stacked-gold.png"` — no `themePresentation` lookup.
  - Contrast with its own sibling two lines down, same brand cluster, same file: `<StampPressIn placement="footer" size={48} />` → `components/atoms/Stamp.tsx:62-64` (`hankoSrc`) and `components/brand/Wordmark.tsx:74` (`src={themePresentation.wordmark}`) both correctly resolve per-theme SVGs (`hanko-{gold,blue}.svg`, `hokuten-wordmark-{gold,blue}.svg`). The footer lockup is the one brand mark in this cluster that does not.
  - Confirmed reproducible, not hypothetical: `git ls-tree -r --name-only origin/theme-blue -- site/public/brand/` lists only `lockup-stacked-gold.png`/`.svg` and `lockup-linear-gold.svg` — **no blue lockup asset exists on either branch** (`theme-blue` is a zero-code-diff branch per `lib/theme.ts`'s own header comment, so this is not a "not yet merged" situation — it will ship as-is). `lib/theme.ts`'s `THEME_PRESENTATION` record has no `lockupStacked`/equivalent field for either theme to key off of.
  - `docs/design/specs/footer.md` §"Finding carried into this spec" (lines 35-39) already logged a *contrast* P0 on this same asset (pixel-sampled `--ink` on `--dark` = 1.04:1, mitigated with the `surface-card` chip) but never raised the dual-theme gap — the spec's own acceptance checklist (line 65: "Only `lockup-stacked-gold.png` is referenced — never `lockup-linear-gold.svg` — anywhere in this file") is written gold-only, with no Theme B counterpart named at all.
  - **Not fixed here**: the correct fix needs either a new `lockup-stacked-blue.{png,svg}` brand asset (asset-authoring, not a component edit) or a `lib/theme.ts` field to key off it (that file is explicitly off-limits to this task: "Never edit ... anything under `lib/` ... unless it is explicitly assigned to you. Report needed changes instead."). Swapping the raster PNG for the already-theme-branched `Wordmark variant="lockup"` SVG was considered and rejected: it renders a different mark (wordmark-only vs. the stacked "Hokuten × KW Commercial" compound lockup this cluster is specced to show) and would be an unreviewed design substitution, not a token fix. Flagging for Razim / design-director + a dated PROJECT-MEMORY.md decision, per the brief's "if a needed token genuinely does not exist, report it rather than inventing one" principle extended to this asset.

**P1 (hierarchy/spacing):** None found within this gate's scope. Both scoped utilities are used correctly and nowhere else: `star-grain` appears only on `.surface-dark` (`components/sections/MethodSection.tsx:122`); the plate-frame device (via the `PlateChrome` component, which deliberately reimplements the `plate-frame` utility's exact spec — hairline border, 9px marks, 1px `--accent-text` stroke, 55% opacity — as four corners instead of two) appears only inside `.surface-paper` (`components/hero/HeroPlate.tsx:90,122`). No section root anywhere hardcodes a surface colour outside the five `.surface-*` scopes — every `<section>`/`<footer>` root in `components/sections/` and the hero chassis uses `surface-paper`/`surface-deep`/`surface-card`/`surface-dark`/`surface-black`.

**P2 (polish):** None found within this gate's scope.

**Reviewed, cleared (not violations, logged for audit transparency):**
  - `components/art/AsciiCanvas.tsx:227` (`` `rgb(${r}, ${g}, ${bl})` ``) — matches the mechanical `rgb(` hunt pattern, but is a runtime blend of two colours read live off `--art-ink`/`--art-mid`/`--art-light` via `getComputedStyle` (see the function's own doc comment: "no colour literal exists in this file"), required because a `<canvas>` 2D context's `fillStyle` cannot consume a CSS `var()` reference. No hardcoded/invented colour value exists here in either theme.
  - Disabled-state `opacity-55`/`opacity-60` (`ui/button.tsx`, `ui/input.tsx`, `ui/select.tsx`, `ui/checkbox.tsx`) and icon-crossfade `opacity-0`/`opacity-100` (`ui/accordion.tsx`, `motion/CopyButton.tsx`) — uniform-element disabled dimming and icon-swap transitions, not text-colour faking; the tokened `text-fg-muted`/`text-fg-meta` are not being bypassed to render body/label copy.
  - `hover:border-accent-text/40` (`cards/TeamCard.tsx:122`, `cards/ClosingCard.tsx:85`, `cards/CardShell.tsx:96,99`) — this is the literal spec value from `docs/AGENT-BRIEF.md` Motion law ("ring shifts to a hairline accent ~40%"), applied to a border, not text.
  - `bg-ink/60` scrim (`ui/dialog.tsx:196`) and `bg-card/90` caption backing (`art/PlateChrome.tsx:72`) — background-opacity on non-text chrome (modal scrim, caption plate), both on documented, ink-tinted semantic tokens, not raw colour.
  - `opacity-[0.55]` registration marks (`art/PlateChrome.tsx:85`) and `opacity: 0.08` orbital arcs (`art/OrbitalArcs.tsx:66`) — decorative, `aria-hidden` art, matching the exact numeric values the `plate-frame`/`star-grain` utilities themselves use in `globals.css` (0.55 / 0.08) — not invented values.

**Verdict:** fail (one P0)

---

**Artifact:** The content-fidelity gate — `site/content/*` and every string rendered by `site/components/sections/*` (closings, listings, stats, team, mandates, method, FAQ, doors, brands, calculator chrome, BOV chrome, footer), audited against the read-only source of record `~/Documents/Dino/dino-sites/kwc-dinomonteverde/` (`index.html`, `marketplace.html`), the extracts in `docs/port/`, and the claims register in `references/06-content-and-proof.md`.
**Date:** 2026-08-08

**P0 (blocker):** none. Specifically cleared, each against the named evidence:
- All six closings match `index.html:872–907` exactly on name, location, segment, note, the two-slot mono metrics line and price — including the source's own inconsistent decimal precision (`$50.1M` beside `$61.49M`) and its slot substitutions (Brooklyn `Confidential · $227K/key`, Rohnert Park `Lease → Buy · 1 year`). One declared unit normalization, now documented — see P2 #1.
- All five listing Crexi URLs are byte-identical to the source `CREXI_LINKS` map (`index.html:1775–1779`); all five match `^https://(www\.)?crexi\.com/`; each slug names the property its card names. `isTrustedCrexiUrl` (`content/listings.ts:48`) ports the source's own public-card guard (`index.html:1836`) and `ListingCard.tsx:66` gates the anchor on it, so a card can never resolve to the private a100 host. NOT VERIFIED: live listing status — crexi.com returns HTTP 403 to automated fetches, so correspondence is established from the source slugs and ref 06, not from the live pages.
- Stats: `$200M+ · 12 · 836K+ · 3×` all reach the DOM server-side (`StatsSection.tsx:81`, plain strings through `CountUp`), and both register qualifiers render — the `11 + 1` decomposition and the three CoStar quarters `Q3 '25 · Q1 '26 · Q2 '26` (`StatsSection.tsx:84`). No `$0 B+` JS-off failure class present.
- Team: Dino's creds line is verbatim from `index.html:1137`; his `CA DRE #01948432` renders at `cards/TeamCard.tsx:150`. Razim's and William's bios (`content/team.ts:62,70`) contain zero numbers, zero awards and zero licence claims. No Sarhan branding, no "~$1B" narrative, no KW corporate award anywhere.
- Mandates: exactly four cards, and every headline and criteria clause verified present verbatim in `marketplace.html` by literal-string grep (deck line :217, cards :222, :227, :242, :277). The two declared departures are correct and load-bearing: the "beverage-industry" descriptor dropped (flag F-05) and the `8x–10x` clause case change.
- Method: five steps render, the reach stats keep their approximation markers intact (`~400K`, `~60K` vs the un-marked `1,500`, `30K`), and the BOV promise is a single exported constant (`content/methodology.ts:43`) that carries its T-12 / STR / PIP condition. Every one of the five places the 48-hour clock appears renders it conditioned: `methodology.ts:44`, `doors.ts:51`, `faq.ts:42`, `hero/heroContent.ts:85`, `app/layout.tsx:34`. Nothing ships an unconditioned clock.
- FAQ: all five `[PLACEHOLDER:confirm — …]` markers survive to the DOM and render as an unmissable `text-brick` hairline block with an `AlertTriangle` and a "Placeholder — confirm before launch" caption (`FaqSection.tsx:74–89`). None dropped, none filled with invented text.
- Voice: zero banned words in rendered copy (the only `unlock`/`seamless`/`Learn more` hits repo-wide are code comments); zero exclamation marks; zero `N/A`; the sole price fallback is `PRICE_ON_REQUEST = "Price on Request"` (`lib/utils.ts:17`). The `I`/`my` occurrences are the visitor's voice in FAQ questions and the byte-exact TCPA block — no Dino-singular voice in non-legal copy. Zero "Hakuten" misspellings.
- Headlines: exactly one italic accent word each, across all eleven (`*sale*`, `worth`, `*desk*`, `six`, `*closer*`, `*working*`, `worth`, `*house*`, `*quietly*`, `*math*`, `defend`). `SectionHeader` warns in dev on a second marker.
- Assets: all six closing photos are md5-identical to the source files, and each `photoAlt` was checked against the actual rendered image — every one accurately describes its subject (Carte's glass tower and the street running to the waterfront; Reno's lit riverfront restaurant terrace; the Last Hotel's stone facade and lit upper cornice; the HIE entrance canopy and lit lobby; McAllen's blue-and-white wing and porte-cochere; the Rodeway Inn's lit porte-cochere at dusk beside the two-story wing). No fabricated alt text.
- Compliance strings verified byte-exact against source by literal grep: the two-sentence brokerage disclosure, all three calculator disclaimers, the benchmark-band scope note, and the `sms_consent_text` audit string (raw `&`, `index.html:1174`).

**P1 (hierarchy/spacing):**
1. **A dangling section index `07` with no `04`–`06` on the page.** `BovSection` defaults to `index = "07"` (`components/sections/BovSection.tsx:82`) and `app/page.tsx:63` renders `<BovSection />` without overriding it. The rendered micro-label sequence down the page is `[ TRUST METRICS ]`, `[ FLAGS WE TRANSACT ACROSS ]`, `[ 01 — TRACK RECORD ]`, `[ 02 — HOTELS FOR SALE ]`, `[ 03 — VALUATION ]`, then five unindexed labels (`[ METHOD ]`, `[ THE OWNER / THE INVESTOR ]`, `[ CAPITAL & MANDATES ]`, `[ THE PRINCIPALS ]`, `[ DILIGENCE FAQ ]`), then `[ 07 — BROKER OPINION OF VALUE ]`. The `07` asserts a position in a sequence the document does not contain. `BovSection.tsx:70–77` documents the number as an explicit assumption, not a verified value. Fix: either drop the default so `#bov` ships unindexed like its five neighbours, or have the page assign the full run. Escalated — both files are outside this task's write scope.

**P2 (polish):**
1. **Undeclared unit normalization in the closings data.** The source's Last Hotel card reads `142 rooms` (`index.html:885`) where its other two counted cards read `keys`; `content/closings.ts` types `keys: 142` and `cards/ClosingCard.tsx:51` renders `142 keys`. The value is correct (keys and rooms are the same quantity, and the source's own listing renderer normalizes identically at `index.html:1810`), but the file header claimed a byte-for-byte re-verification while enumerating only the inconsistencies it *preserved*. FIXED — the normalization is now declared in the header with its three justifications, so the fidelity claim is honest and a future auditor does not re-flag it.
2. **Listing 5 shipped the source's shorthand comment as its display name.** `content/listings.ts` had `name: "Baymont Jacksonville Airport"` (the trailing code comment at `index.html:1779`) while ref 06's "Seed content sources" line, the Crexi slug `florida-baymont-by-wyndham-jacksonville-airport`, and *this row's own `photoAlt`* all gave the full "Baymont by Wyndham Jacksonville Airport" — the row disagreed with itself, and the card rendered "Baymont" twice (title + `brand` in the meta line). FIXED — full name adopted, redundant `brand` dropped, so the flag is stated exactly once per card.
3. **The same registered claim rendered with two wordings on one page.** `#stats` renders the CoStar award as label "CoStar Power Broker" + `Q3 '25 · Q1 '26 · Q2 '26` (correct — verbatim from the source trust strip, `index.html:861`), while Dino's bio renders "Three-time CoStar Power Broker Quarterly Deals winner — Q3 2025, Q1 2026, and Q2 2026" (correct — verbatim from `index.html:1137`). Both are faithful ports of two different source strings, so neither is drift; noted only so a future pass does not "harmonize" them and break both ports.
4. **`$200M+` carries two different meanings on one page.** `#stats` renders it as aggregate closed volume; `#mandates` card 3 renders it as a buyer's deployment budget (`content/mandates.ts:45`, flagged F-16 in that file). The mandate's context disambiguates ("Investment group targeting select-service and above"), but the collision is real and is worth a decision before public launch.
5. **Two stat figures are retyped outside `content/`.** `app/layout.tsx:34` restates `$200M+` and `12 hospitality transactions` in the metadata description rather than importing `content/stats.ts`, and it drops the register's `11 + 1` qualifier. Content law says import, never retype. Escalated — `app/layout.tsx` is outside this task's write scope.
6. **`#closings` states the `12` without its qualifier.** `ClosingsSection.tsx:33` renders "12 closed transactions — six shown in full." `content/stats.ts`'s own rendering contract says the register's detail "must render wherever their value does". Mitigated in practice: `#stats` renders the `11 + 1` decomposition and sits four sections earlier in document order. Escalated rather than edited — section chrome is another agent's file.

**Verdict:** pass (no P0; one P1 escalated, two P2s fixed in `site/content/`)

---

**Artifact:** The accessibility gate — WCAG 2.1 AA treated as binding law (PHASE-1-EXECUTION §8.1: DOJ ADA web rule, CA Unruh statutory exposure, Larchmont sponsoring brokerage). Scope: every file in `site/app` and `site/components`, read end to end, plus `app/globals.css` §5/§7 (focus ring, reduced motion, marquee pause, `visually-hidden`, `skip-link`), `content/nav.ts` + `content/site.ts` (anchor targets), and `@radix-ui/react-select@1.x` / `react-dialog@1.1.23` in `node_modules` for the primitives' real ARIA output.
**Date:** 2026-08-08

**P0 (blocker):**

1. **Both marquees auto-scroll with no pause mechanism reachable by keyboard or touch — WCAG 2.2.2 Pause, Stop, Hide (Level A) — and `/accessibility` publicly states the opposite.**
   The token sheet's only pause rule is `[data-marquee-viewport]:hover, [data-marquee-viewport]:focus-within { animation-play-state: paused }` (`site/app/globals.css:631-635`, comment: "a11y: WCAG 2.2.2"). `:focus-within` can only match if something inside is focusable, and neither rail contains a focusable node: `#brands` renders bare `<span>` marks (`site/components/sections/BrandsSection.tsx:83-89`) and `#ticker` renders `<span>` label/value pairs (`site/components/ticker/TickerClient.tsx:102-118`). The viewport itself was a plain non-focusable `<div>` (`site/components/motion/Marquee.tsx:81-88`). Both rails auto-start, loop 40s / 45s (> 5s), and run in parallel with other content — 2.2.2 applies to every user, not only the reduced-motion cohort that `globals.css:621-628` already serves. Compounding it: `site/app/accessibility/page.tsx:84` publishes "The franchise-flag marquee and the market ticker stop when a pointer rests on them **and when anything inside them receives keyboard focus**", which was false as built — and that same file's own honesty rule (`:14-19`) names an overstated statement as "an admission written in our own hand" under Unruh.
   FIXED in `site/components/motion/Marquee.tsx`: the labelled `role="group"` viewport now carries `tabIndex={0}` (standard focusable-region technique), so it is announced by its `aria-label`, takes the global 2px ring from `globals.css:364`'s `[tabindex]` selector, pauses the track while focused, and traps nothing.
   SECOND DEFECT FOUND WHILE FIXING THE FIRST — `rail-mask` (`globals.css:458-467`) is a `mask-image`, and a CSS mask applies to an element's whole rendering including its outline, so the new focus ring would have faded to transparent across the outer 8% at each end. `rail-mask` and `overflow-hidden` have been moved onto a new inner clipper; the focusable viewport is now unmasked and its ring paints in full. Visual output is unchanged (the clipper is `w-full`, same box the mask sized to before).
   ESCALATED, needs a design decision: a visible pause/play control is the stronger 2.2.2 remedy. Focus-to-pause is discoverable only by tabbing into the rail.

2. **The landing route's `<main>` is not focusable, so the skip link does not move focus in WebKit — WCAG 2.4.1 Bypass Blocks (Level A).**
   `site/app/layout.tsx:78` ships `<a href="#main" className="skip-link">`; `site/app/page.tsx:51` renders `<main id="main">` with no `tabIndex`. WebKit does not set the sequential focus navigation starting point on same-document fragment navigation, so on iOS/macOS Safari the skip link scrolls but leaves focus on the link — the next Tab re-enters the nav it was supposed to bypass. iPhone SE is a mandated QA viewport (AGENT-BRIEF "Responsive"), so this is the primary-browser path, not an edge case. The codebase already knows the remedy and applies it on the other three routes: `site/components/legal/LegalPage.tsx:222` renders `<main id="main" tabIndex={-1}>` with the comment "tabIndex -1 makes the landmark focusable so the skip actually moves focus."
   ESCALATED, not fixed — `app/page.tsx` is outside this task's write scope. Exact fix: `site/app/page.tsx:51` → `<main id="main" tabIndex={-1}>`.

3. **The hero scroll cue is a ~39px tap target, below the 44px floor.**
   `site/components/hero/HeroCoverPanel.tsx:139-145` and `site/components/hero/HeroPlate.tsx:157-163`. Measured from the tokens: `micro-label` at `--text-micro` `0.6875rem` / line-height `1.4` = 15.4px (`globals.css:47-49`), plus `gap-2` 8px, plus `size-4` 16px = 39.4px. Both files' own comments already state "the cue's own rendered height (~39px)". Ref 07 P0: "tap target <44px".
   FIXED in both files with the codebase's own transparent hit expander (`ui/button.tsx:36` `HIT_EXPANDER`): `before:absolute before:content-[''] before:inset-x-0 before:-inset-y-1` → ~47px hit area. The painted box is unchanged, so the documented `bottom-6` / `section-pad` 64px-floor clearance maths in those comments still holds. The anchor is already `absolute`, so it is its own containing block.

**P1 (hierarchy/spacing):**

1. **Anchor-click focus management is implemented on one path to a section and not the other.** `SiteNav`/`MenuOverlay` move DOM focus to the target heading on every internal anchor click (`site/components/sections/SiteNav.tsx:118-147`, `site/components/nav/MenuOverlay.tsx:74-91`), and `SiteNav.tsx:337` exports `handleAnchorClick` explicitly "so a caller can reuse the exact focus-management contract (e.g. the eventual Hero CTA)". The hero never took it up: `HeroCoverPanel.tsx:107-112, 139` and `HeroPlate.tsx:111-116, 157` are plain `<a href="#bov">` / `#closings` / `#stats` with no handler, so the same destination behaves differently depending on which control the user pressed — and in WebKit (see P0 #2) neither native focus movement nor the starting-point fallback fires at all. NOT FIXED: both hero chassis are Server Components, and wiring the handler forces a `"use client"` boundary onto the hero, which is a JS-budget/architecture decision (AGENT-BRIEF: 180KB landing route), not an audit call. Recommended remedy: a small shared client `<AnchorLink>` island under `components/nav/` used by the hero CTAs and the scroll cue, with `SiteNav`/`MenuOverlay` collapsing onto it.

2. **The sticky nav and the fixed ticker are mounted inside `app/template.tsx`'s transform wrapper, contradicting both components' own "not negotiable" contracts.** `site/app/page.tsx:49` renders `<SiteNav />` and `:67` renders `<TickerBar />`, both inside the page — and `template.tsx` wraps every page. `site/app/template.tsx:21-28` states: "While the transition runs, this wrapper carries a transform, which makes it the containing block for any `position: fixed` descendant. The fixed `#ticker` and the sticky nav must therefore live in app/layout.tsx, OUTSIDE this template." `site/components/ticker/TickerBar.tsx:10-17` repeats it: "Last element inside `<body>` in `app/layout.tsx` … Not negotiable." Effect: on a client-side navigation back to `/` from `/privacy`, `/sms-terms` or `/accessibility`, the ticker detaches from the viewport and the nav loses its sticky containing block for the 300ms transition. ESCALATED — `app/page.tsx` and `app/layout.tsx` are outside this task's write scope. Fix: move `<SiteNav />` and `<TickerBar />` into `app/layout.tsx` around `{children}`.

**P2 (polish):**

1. **`CardShell` draws a card-wide focus ring for *any* focused descendant anchor, so a secondary link inside a card gets two rings.** `site/components/cards/CardShell.tsx:99-100` uses `has-[a:focus-visible]:outline-2 …` unscoped, while `:114` only suppresses the ring on the stretched title anchor. `site/components/cards/ListingCard.tsx:94-99` renders a second real link ("Email us about this listing", `relative z-2`) whenever the Crexi URL fails `isTrustedCrexiUrl` — focusing it paints its own global ring *and* the whole-card outline. Focus is still visible (so not a 2.4.7 failure), but the indicator no longer identifies what is focused. Not fixed: the precise remedy is to mark the stretched anchor (e.g. `data-card-link`) and narrow the selector to `has-[a[data-card-link]:focus-visible]`, which touches `components/cards/` and is worth more than a P2's risk budget while other agents are live in that directory.

2. **The footer's stacked lockup ships `alt=""` while acting as the footer's primary brand mark.** `site/components/sections/SiteFooter.tsx:108-114`. The only text equivalent, `BRAND_LINE`, is ~70 lines further down at `:176`, past three navs and the disclosure — so an AT user reaches the compliance block before the brand is ever named. `site/components/brand/Wordmark.tsx:31-36` sets the opposite house rule for exactly this case: "a standalone lockup IS the only rendering of the brand name in its placement, so the image needs a real accessible name." Not fixed: whether that mark is decorative-beside-`StampPressIn` or the brand's accessible name in the footer is a brand decision (ref 01 "Lockups & usage"), not an audit call.

3. **`#brands`'s accessible name includes its bracket punctuation.** `site/content/brands.ts:68` freezes `BRANDS_MICRO_LABEL = "[ FLAGS WE TRANSACT ACROSS ]"` and `site/components/sections/BrandsSection.tsx:74` renders it verbatim into the `<h2 id="brands-heading">` that `<section aria-labelledby>` points at — so the section announces as "left bracket flags we transact across right bracket". Every other micro-label on the site avoids this: `atoms/MicroLabel.tsx:44-52` composes the brackets into `aria-hidden` spans precisely "so a screen reader hears '01 Track record'". Not fixed: `BrandsSection.tsx:63-73` documents at length why the frozen compliance string must not be re-derived in the component. Remedy belongs in `site/content/brands.ts` — export the words separately from the bracket scaffolding and let `MicroLabel` compose them, as every other section already does.

**Cleared under this gate (checked, not assumed):** exactly one `<h1>`, owned by the hero (`HeroCoverPanel.tsx:96` / `HeroPlate.tsx:100`; `LegalPage.tsx:227` owns the legal routes' own single `h1`), and heading order runs h1 → h2 → h3 with no skipped level on any route · all twelve landing sections are `<section id aria-labelledby>` pointing at a heading carrying that exact id, and all eight nav/menu anchors (`content/nav.ts:24-58`) resolve to a real `{id}-heading` · `header`/`nav`/`main`/`footer` landmarks correct, every `<nav>` distinctly named ("Primary", "Site menu", "Footer navigation", "Policies") · zero `outline-none` outside `CardShell.tsx:114`, where the ring is deliberately relocated to the card via `has-[a:focus-visible]` · MenuOverlay and ConsentModal inherit Radix's focus trap, scroll lock and focus restore, and `MenuOverlay.tsx:134-141` correctly distinguishes navigation-close (focus → target heading) from Esc/outside-close (focus → trigger) · the consent bar refuses outside-click and pre-interaction Esc with shake + haptic + a live-region notice announced in both motion modes (`ConsentModal.tsx:105-120, 184-199`) · every animated component gates on `useReducedMotion()` **and** `motionAllowed()` with a designed static state (`Reveal`, `CountUp`, `Stamp`, `AsciiCanvas`, `SmoothScroll`, `dialog`, `select`, `checkbox`, `accordion`, `BenchmarkBars`, `template`), and `Marquee`/`CopyButton`/`Button` are CSS-only with `globals.css:606-642` as their static state · every form control has a real `<label>` (`ui/field.tsx:95`, `ui/checkbox.tsx:107`, `CalculatorSteps.tsx:158`, `CalculatorResult.tsx:473`) — no placeholder-as-label anywhere; errors are icon + text + `role="alert"` + `aria-describedby`, never colour alone (`field.tsx:107-121`, `CalculatorSteps.tsx:172-181`); `autocomplete` present on name/organization/email/tel-national/postal-code and deliberately `off` on the two fields where a browser suggestion would corrupt the value; the BOV error summary is focused on submit failure with per-field links (`BovForm.tsx:268-275, 373-404`) · CityPicker implements full ARIA 1.2 combobox semantics with `aria-activedescendant`, 44px options and a polite result count (`CityPicker.tsx:343-437`) · all decorative art is `aria-hidden` with an adjacent `visually-hidden` description where it depicts something (`AsciiStatic.tsx:95-97`, `AsciiCanvas.tsx:1115-1116`, `HotelEngraving.tsx:206`, `MethodSection.tsx:125-128`) and without one where it is pure geometry (`OrbitalArcs`, `PlateChrome` marks, `AccentRule`) · every photo goes through `PhotoFrame`, which makes `alt` type-required, forces explicit dimensions or `aspect`+`sizes` for the CLS budget, and warns in dev on treatment-describing alt (`PhotoFrame.tsx:98-136`) · no hover-only information: the `photo-reveal` grayscale→colour carries no meaning by construction, `ui/button.tsx:72` pins the link underline on `(hover:none)`, and every "opens in a new tab" warning is real `visually-hidden` text · no raw colour bypasses the tokens — the only non-token values are the four documented `color-mix()` contrast repairs in `ui/field.tsx`, `ui/input.tsx` and `ui/button.tsx`, each carrying its measured on-dark ratio.

**Verdict:** fail (three P0; two fixed here, one escalated to `app/page.tsx`)

---

**Artifact:** The anti-AI-slop + design-coherence gate — all 13 rendered sections of `app/page.tsx` (`#hero` → footer + `#ticker`), read section by section against ref 07 "Anti-AI-slop gates", ref 03 (type ramp, colour roles, density, card grids), ref 04 (per-section anatomy) and PHASE-1-EXECUTION §2 (three registers) / §3 (typography program). Scope covered the section components plus the atoms/cards/calculator/form leaves they render.
**Date:** 2026-08-08

**Headline:** the page does NOT read as generated output. Every named slop tell is absent, verified mechanically, not by impression:
- No centred-gradient hero + three feature cards (`HeroCoverPanel.tsx:90` is a left-aligned two-column grid over a flat `--black` panel; no gradient utility exists in `components/`).
- No purple/violet glow, no default Tailwind palette, no raw hex — the only `rgb(`/hex hits in `components/` are `art/AsciiCanvas.tsx:227` (a runtime blend of live `--art-*` values, already cleared in the token audit above) and a `#cFb` format-code comment.
- No glassmorphism without cause: the single `backdrop-blur` sitewide is `SiteNav.tsx:272`, gated on `scrolled`, which is ref 04's literal spec ("paper with blur on scroll").
- No stock or AI photography (six track-record hotels + one real portrait); the `#method` engraving is commissioned line-art, expressly permitted by ref 03 Imagery.
- No emoji anywhere; the only `→` in rendered copy are inside mono micro-label type (`mandatesCta.label`, `menuUtilities.privateAccess`) — ref 03's sanctioned exception.
- No fake/rounded metrics: every figure traces to a `verified-current` register row, and the reach stats keep their honest approximation markers (`~400K`, `~60K` vs unmarked `1,500`, `30K`).
- No generic CTAs ("Request a written BOV", "See the track record", "See current listings", "Request invite to a100 Arms"); zero banned words in rendered strings.
- Not Inter-only: three real voices, and Fraunces loads a true italic file (`app/layout.tsx:14`).
- No dark-mode toggle (grep for `dark.mode|toggleTheme|theme-toggle|prefers-color-scheme` in `app/` + `components/` returns nothing) — the two themes are a build-time env selection, not a user control.
- Card grids never translate, never scale, never lift a shadow on hover (grep for `hover:translate|hover:scale|hover:shadow` across `components/` returns nothing); only `photo-reveal` moves, and only the photo.

**P0 (blocker):** one, and it is a launch-gate item, not a code defect.

1. **`#faq` renders five live "Placeholder — confirm before launch" notices.** Gate violated: ref 07 anti-slop ("the built page must not read as unfinished") + ref 07 P0 ("a `pending-verification` claim rendered live"). Evidence: `content/faq.ts:56,63,71,79,88` each carry a `[PLACEHOLDER:confirm — …]` marker, and `components/sections/FaqSection.tsx:74–89` renders each one as a `text-brick` hairline block with an `AlertTriangle` and the caption "Placeholder — confirm before launch". A visitor expanding "What are your fees?" is shown "Placeholder — confirm before launch: commission structure, marketing-cost allocation, listing term and exclusivity…"; the licensing answer's marker states outright that "the KW / Forward Wilshire paperwork gate must clear before this answer ships publicly under the Hokuten name."
   NOT FIXED, deliberately. Deleting the notices would ship the shortened answers as if complete, which is strictly worse than showing the gap, and the device is the correct pre-launch behaviour. Mitigation in place: the accordion is `type="single" collapsible`, so at most one notice is visible at a time. This blocks public deploy, not the build. Resolution is content + counsel, not design: fill the five markers, or cut the affected answers, before the site goes public. Owner: `content/faq.ts` (out of this task's write scope).

**P1 (hierarchy/spacing):**

1. **The bracketed index run was incoherent top to bottom — FIXED.** Gate violated: ref 07 P1 ("section missing its micro-label index") + the brief's index-coherence mandate. The concurrent section agents each guessed with no shared registry, so the page shipped `[ TRUST METRICS ]`, `[ FLAGS… ]`, `01`, `02`, `03`, `[ METHOD ]` (unindexed), then **`[ 01 — THE OWNER ]` / `[ 02 — THE INVESTOR ]` restarting the count mid-page** (`DoorsSection.tsx:42` rendering `door.index`), three more unindexed labels, and finally a dangling `[ 07 — BROKER OPINION OF VALUE ]` (`BovSection.tsx:82`, self-documented as an assumption). A numbering device that resets and then skips is worse than no numbering — it reads as decoration, which is exactly the slop tell.
   **The definitive sitewide sequence, now fixed in code:** ref 04 pins `#closings` to `01`, so nothing above it may take a number. `#hero`, `#stats` and `#brands` are the masthead band and stay word-only (`[ HOSPITALITY INVESTMENT SALES — NATIONWIDE ]`, `[ TRUST METRICS ]`, `[ FLAGS WE TRANSACT ACROSS ]` — the last is a frozen compliance constant). The numbered chapter run is then unbroken:
   `01 #closings · 02 #listings · 03 #calculator · 04 #method · 05 #doors · 06 #mandates · 07 #team · 08 #faq · 09 #bov`.
   Edits: `MethodSection.tsx:139` (`index="04"`), `DoorsSection.tsx:93` (`index="05"`) with the panel-level `01`/`02` dropped at `:42` (`door.index` retained in `content/doors.ts` for a future standalone route), `MandatesSection.tsx:47` (`06`), `TeamSection.tsx:42` (`07`), `FaqSection.tsx:113` (`08`), `BovSection.tsx:82` default `07` → `09`. The registry is written into `MethodSection.tsx`'s and `BovSection.tsx`'s headers so the next agent cannot re-guess. This also closes the P1 escalated by the content-fidelity audit above.

2. **A second, conflicting numbered index still ships in the menu overlay — ESCALATED.** `content/nav.ts` `menuItems` numbers its eight destinations `01 The Group · 02 Listings · 03 Track Record · 04 Valuation · 05 Method · 06 Mandates · 07 Team · 08 Contact`, rendered at `components/nav/MenuOverlay.tsx:176`. That contradicts the page: the overlay says `03 Track Record` and the section says `[ 01 — TRACK RECORD ]`; `04 Valuation` vs `[ 03 — VALUATION ]`; `05 Method` vs `[ 04 — METHOD ]`. Ref 04 specifies both mappings and therefore contradicts itself. Two numbering systems on one page is the same failure as one broken system. Recommended resolution: reorder `menuItems` into DOM order and adopt the section numbers (`01 Track Record · 02 Listings · 03 Valuation · 04 Method · 05 Two Doors · 06 Mandates · 07 Team · 08 Diligence · 09 Contact`, with `The Group` → `#hero` carried unnumbered at the head). Not fixed here — `content/nav.ts` is outside this task's write scope, and reordering the menu is a wayfinding decision.

3. **`#brands` used a third tracked-caps flavour — FIXED.** Gate violated: typography program, "tracked caps come in exactly two flavours." `BrandsSection.tsx:53` set the nine franchise marks in `font-sans … uppercase tracking-micro` — Inter caps at 0.14em, which is neither `brand-line` (Inter 0.35em) nor `micro-label` (mono 0.14em), and at `text-heading` (22→28px) it was the largest and most conspicuous tracked-caps run on the page. Changed to `tracking-brand`, adopting the `brand-line` flavour via its face + tracking tokens while keeping `text-fg-meta` (the `brand-line` utility itself hard-sets `color: var(--accent-text)`, which would colorize marks ref 01 requires stay grayscale).

4. **`#bov` spent the italic device on a ~60-word paragraph — FIXED.** Gate violated: typography program, italic is a scarce one-word display accent. `BovSection.tsx:115` set the whole closing disclaimer as `font-display … font-light italic text-fg-muted`. A long italic serif run at muted contrast is the "wedding invitation" tell and devalues the eleven one-word headline accents that are the site's actual signature. `italic` dropped; the Fraunces face plus the `border-l-2 border-accent` rule already separate it from the form beside it.

5. **Six faux-oblique Inter runs — FIXED.** Gate violated: typography program, "never italicise UI or data", compounded by a real rendering defect — `app/layout.tsx:19` loads Inter with **no italic style** (only Fraunces declares `style: ["normal", "italic"]`), so every `italic` on `font-sans` was rendering a browser-synthesized oblique. Sites: `calculator/InfoPopover.tsx:60` (the example line) and `:279` (`<em>sold</em>`), `calculator/CalculatorResult.tsx:141` (the `<em>` branch of the insight mini-markup renderer), `:277` and `:283` (the used-defaults / NOI-override qualifiers), `forms/CityPicker.tsx:404` (the no-results row). All replaced with ref 03's sanctioned hierarchy moves — weight (`not-italic font-medium`) or colour (`not-italic text-fg-muted`), with `<em>` semantics preserved and `not-italic` added to defeat the UA default. Note for whoever owns `app/layout.tsx`: adding an Inter italic file would be a legitimate alternative and stays inside ref 05's ≤2-files-per-family budget, but the fix above is the one the typography program actually wants.

6. **Three-register test — five sections carry ZERO mono data, one carries zero of anything but data.** Method: for each section I enumerated the mono/`data-line`/`tabular` content actually rendered, not the tokens available.
   - **`#hero` — no data proof.** Heritage ✓ (ASCII canvas). Mono type ✓ but it is all *labels* (eyebrow, value rail, scroll cue) — the first full viewport of the site contains no number. `heroContent.ts:18–20` documents the choice ("#stats is the very next section and owns those numbers"), and the reasoning is sound about *duplication*, but the thesis is per-viewport. Recommendation, not applied (it needs a content decision, and `heroContent.ts` carries a deliberate documented call): one mono proof line under the CTAs — the T-12/STR/PIP receipt condition already in `sub` set as a `data-line`, which invents nothing.
   - **`#doors` — zero mono data.** `DoorsSection.tsx` renders micro-labels, a serif headline, an accent rule and sans prose. Brochure drift, in the one section that makes the site's two commercial promises. Recommendation: a mono `DataLine` per panel (Owner: the 48h / T-12 / STR / PIP terms; Investor: the confidentiality categories) — both already exist as prose in `content/doors.ts:51,60`, but promoting them to a data slot needs a new field on that content module, which is outside this task's write scope.
   - **`#faq` — zero mono data.** `ui/accordion.tsx:102` sets triggers in Fraunces; answers are Inter body. Deliberately NOT fixed by numbering the questions: that would put a second bracketed index inside a section that already carries `[ 08 — DILIGENCE FAQ ]`, recreating exactly the failure fixed in P1 #1.
   - **`#bov` — zero mono data.** `BovForm.tsx`, `ui/field.tsx`, `ui/input.tsx` and `ui/checkbox.tsx` contain no `font-mono` / `data-line` / `micro-label` at all (verified by grep). The section's only mono is the header's micro-label.
   - **`#brands` — zero mono data, zero heritage.** Acceptable: it is a band, not a chapter, and the register load is carried by the sections either side.
   - **`#mandates` — zero warmth AND zero heritage.** Dark surface, no photography, no art object (deliberately, so it does not repeat `#method`), hairline cards, mono criteria. It is the section that reads most like a terminal. Correctly NOT solved with a second art object — `MandatesSection.tsx:9–12` is right that `#method` owns the page's dark-chapter art. The available warmth lever is typographic air, not imagery. Flagged for a design decision, not changed.
   - Passing cleanly on all three registers: `#stats`, `#closings`, `#listings`, `#calculator`, `#team`, and `#method` (dark by design, but carries heritage art + mono data).

7. **`#method`'s reach-stat labels were 15-word sentences set in 16px mono — FIXED.** Gate violated: ref 03 Data role (mono is for figures) + hierarchy (mono value at `text-body-lg`/500 beside a mono label at `text-body`/400 is barely a step). `MethodSection.tsx:183` → `font-sans`. Ref 04's "reach stats row in mono" is satisfied by the values, which stay mono/tabular; face contrast now carries the value/label step alongside weight and colour, and the chapter reads markedly less like a console.

8. **`#listings` broke the card-grid rule at `sm` — FIXED.** Gate violated: ref 03 Spacing ("2-up only ≥640px if cards stay ≥320px wide") + consistency. `ListingsSection.tsx:50` went 2-up at `sm` (640px), where `container-hk` leaves 592px and a 32px gap yields **280px** tiles. Changed to `md:grid-cols-2` (768px → 344px), which also aligns it with `#closings` and `#team` so all three card grids now break at the same viewport instead of one stepping early. `ListingCard.tsx:83` and `TeamCard.tsx:132` `sizes` hints updated from `640px` to `768px` to match their real grids.

9. **`#brands` claimed chapter rhythm for a 28px logo row — FIXED.** Gate violated: ref 03 density ("one idea per screen"). `BrandsSection.tsx:65` carried `section-pad`, which at 1440px puts ~147px above and below the marquee — roughly a 10:1 void-to-content ratio, giving a quiet-familiarity strip the same vertical claim as `#closings`, and reading as an accidentally empty screen rather than as air. Ref 04 calls this a "band". Changed to `py-14 lg:py-20` (56/80px, same base-8 scale) with internal `gap-8 md:gap-10`.

10. **`#team` filled a decorative shape with the accent, twice in one grid — FIXED.** Gate violated: ref 03 colour roles ("accent = action/exclusivity only; never decorative fills") + accent scarcity. `TeamCard.tsx:105` rendered the north-star glyph at 40–48px in `text-accent-text`; because three of four rows in `content/team.ts` have no portrait, two of the three principal cards drew that plate side by side — two solid accent stars out-accenting the section's own CTAs and reading as a placeholder badge. Changed to `text-fg-meta`, which on the `surface-deep` plate reads as a quiet watermark. The underlying content gap (two of three principals have no portrait) remains, and is the real fix.

**P2 (polish):**
1. `#mandates` also goes 2-up at `sm` (`MandatesSection.tsx:56`, 284px cards at 640px). Left as-is: ref 03's ≥320px rule names listing/closing cards, and these are text-only tiles with no photo to degrade. Worth aligning to `md` if a sitewide grid convention is ever written down.
2. `#faq` stacks six `text-accent-text` plus/minus icons down its right edge (`ui/accordion.tsx:112`) in a section with no other accent — the highest accent-per-idea density on any light section, though each mark is only 16px. Below the ~5% viewport threshold; noted for the next polish pass.
3. `#team` (paper) runs straight into `#faq` (paper) with no hairline or surface change between them — the only place two full chapters share a ground with nothing marking the seam. `#brands` solves the identical adjacency with `hairline-t hairline-b`.
4. `content/methodology.ts`'s header comment still proposes `[ 03 — METHOD ]`. Stale after P1 #1 — `03` belongs to `#calculator`. Harmless (it is a comment, and the component no longer reads it), but it will mislead the next agent. Out of write scope.
5. `#stats`'s four numerals and its own section headline both render at `text-display2` (`StatsSection.tsx:80` and `SectionHeader.tsx:124`), so the band has two focal steps at identical scale. It resolves in practice because four repeated numerals out-weigh one headline, but nothing in the code makes that intentional.

**Verified by:** `npx tsc --noEmit --incremental false` (clean) and `npx vitest run` (103/103 passing, `lib/valuation.test.ts` — the frozen calculator engine untouched) after all edits.

**Verdict:** fail (one P0 — the five live `#faq` placeholder notices block public deploy; every P1 inside scope is fixed, two escalated)

---

## THE COMPLIANCE GATE — legal strings, TCPA/10DLC, evidence gate, secrets

**Artifact:** `site/content/compliance.ts` · `site/components/sections/SiteFooter.tsx` · `site/components/forms/` · `site/components/modals/` · `site/app/{privacy,sms-terms,accessibility}` · `site/components/seo/` · `site/components/calculator/` — audited against PHASE-1-EXECUTION §8, ref 06, `docs/port/02-compliance.md`, and the read-only source at `~/Documents/Dino/dino-sites/kwc-dinomonteverde/`.
**Date:** 2026-08-08

**P0 (blocker):**

1. **A stored "Reject all" is silently overridden on three of the four routes.** `<Analytics />` and `<SpeedInsights />` mount in `site/app/layout.tsx:82-83`, so both beacons run on every route. The only thing that installs the `beforeSend` gate is `applyConsent()` (`site/lib/consent.ts:277-280` → `installMeasurementGuard()`), and its only caller is `ConsentProvider`'s after-paint effect (`site/components/modals/ConsentProvider.tsx:76-89`). `ConsentProvider` is mounted in exactly one place — `site/app/page.tsx:45` — and `site/components/legal/LegalPage.tsx` mounts none. On `/privacy`, `/sms-terms` and `/accessibility` the module-level `measurementGranted` therefore stays at its default `true` (`lib/consent.ts:211`), no guard is registered, and the visitor is measured with no mechanism to decline. This contradicts the shipped consent copy byte-for-byte — `lib/consent.ts:337`: "They are running now; declining stops them in this browser." Gate: PHASE-1-EXECUTION §8.3 (consent modal + CalOPPA), ref 07 consent P0 family. **Not fixed — mount point is outside scope.** Remedy: wrap `{children}` in `<ConsentProvider>` inside `app/layout.tsx:81` (one mount sitewide, per the "MOUNT IT ONCE" note at `ConsentProvider.tsx:25-26`) and drop the wrapper from `app/page.tsx:45`.

2. **The nationwide-coverage claim ships unqualified and with no register row.** Rendered as `"Hospitality investment sales — nationwide"` (`site/components/hero/heroContent.ts:78`), as `"Hospitality investment sales across the United States."` in the page/OG/Twitter description on every route (`site/content/site.ts:51-52` via `lib/seo.ts`), and machine-readably as `areaServed: {"@type":"Country","name":"United States"}` on both the organisation and the BOV `Service` node (`site/components/seo/JsonLd.tsx:73`, `:174`, `:208`). Ref 06's verified-claims register has **no coverage row at all**, so this fails the evidence gate on its own terms ("A claim ships only when this table has a row for it"). Worse, the source of record qualified the claim in **both** places it made it — `index.html:1152` ("Nationwide referral network and formal partner-brokerage relationships in every U.S. state for out-of-California engagements. Brokerage of record for all listings.") and `index.html:1249`, the footer legal line sitting immediately beside the DRE disclosure. Neither Hokuten footer carries it (`SiteFooter.tsx:152-179`; `LegalPage.tsx:139-178`), so on `/privacy`, `/sms-terms` and `/accessibility` an unqualified nationwide claim renders in the metadata and JSON-LD with no qualifier anywhere on the page. The only surviving qualifier is inside a collapsed FAQ answer on `/` (`content/faq.ts:88`) that itself renders a brick "Placeholder — confirm before launch" notice. Gate: §8.2 "no unlicensed-activity language" + ref 06 evidence gate. **Not fixed — writing new legal copy into the footer is Razim's/counsel's decision, and content law forbids inventing it.** Remedy: add a register row for the coverage claim and a frozen `OUT_OF_STATE_QUALIFIER` constant to `content/compliance.ts` ported from `index.html:1152`, rendered beside `BROKERAGE_DISCLOSURE` in both footers.

**P1 (hierarchy/spacing/consistency):**

3. **A frozen legal string is retyped outside its owner.** `site/content/faq.ts:88` embeds the full two-sentence brokerage disclosure as a raw literal instead of importing `BROKERAGE_DISCLOSURE`. Byte-identical today (verified by literal scan), so nothing mis-renders — but `content/compliance.ts:20` states "Nothing else in the app may retype these strings — import them from here," and this copy is the one that renders inside the licensing FAQ answer. Out of scope (`content/`); fix by importing and interpolating.

4. **The calculator disclaimer has two owners and no equality test.** `content/compliance.ts:194, 202, 206, 210, 219` and `lib/valuation.ts:764-773` (`RESULT_DISCLAIMER`, `RESULT_CONTEXT_BASE`, `RESULT_CONTEXT_DEFAULTS_NOTE`, `RESULT_CONTEXT_NOI_NOTE`) hold the same four frozen sentences. The UI renders the `compliance.ts` copies (`CalculatorResult.tsx:275-289`, `CalculatorSection.tsx:70`); `lib/valuation.test.ts:594, 620-621` asserts only the `valuation.ts` copies. Verified byte-identical today. An edit to `compliance.ts` would ship a diverged disclaimer with a fully green test suite. Out of scope; fix with a parity assertion in `lib/valuation.test.ts` (importing `compliance.ts` from `compliance.ts` into the engine would pull the calculator into the BOV form's client bundle — do not do that).

5. **The footer brand cluster is a KW-dominant lockup, inverting the Hokuten-first guardrail.** `SiteFooter.tsx:107-115` renders `lockup-stacked-gold.png` as placement ① of the brand signature. Inspected the asset (2400×1836): the KW Commercial mark occupies roughly 85% of the artwork and "THE HOKUTEN GROUP" is a tracked-caps line across the bottom ~2% of the image height — at the rendered `h-10 sm:h-12` (40–48px) that resolves to about 1px of cap height, i.e. illegible. Net effect: the KW Commercial logo renders **twice** in the footer (again at `:141-149`, the sanctioned compliance mark) and the footer's brand signature reads KW, not Hokuten. Ref 01:12 — "On the website the hierarchy inverts: Hokuten first; KW Commercial is a footer compliance mark only (decision 2026-08-07)" — and the same AGENTS.md hard guardrail. `docs/design/specs/footer.md:14` prescribes the stacked lockup, so spec and skill reference disagree; AGENT-BRIEF:5-6 says the skill reference wins. **Not fixed:** silently re-litigating a logged spec decision is barred by the decision protocol. `site/public/brand/hokuten-wordmark-{gold,blue}.svg` exist if the call goes the other way.

6. **Ref 07 pre-deploy grep `grep -ri "FRED_API_KEY" app components lib --exclude-dir=api` returns FAIL.** Hits: `site/lib/ticker.ts:17` and `:74`, both JSDoc prose naming the variable, in a module imported by the client component `components/ticker/TickerClient.tsx:59`. There is **no secret exposure** — no value, no `process.env` read; the sole read on the whole site is `app/api/ticker-data/route.ts:172`, and a full-tree scan for UUID-shaped and 32-hex literals, `sk_live`/`pk_live`/`AKIA`/`BEGIN`/`password=`, and Dino's kwc Web3Forms key `26d2a63e-…` returned zero hits. But the gate my brief names ("any hit in a module a client component imports") and the ref 07 grep are both binary and currently red. Out of scope; fix by rewording the two comments to say "the FRED key" without the literal token.

**P2 (polish):**

7. **Ref 07 pre-deploy grep `grep -ri "sarhan" app components content lib` returns FAIL** — three JSDoc hits (`components/sections/StatsSection.tsx:6`, `components/brand/Wordmark.tsx:9`, `components/motion/CountUp.tsx:6`), each citing the Sarhan site as an anti-pattern. No Sarhan branding renders anywhere. Reword to close the gate.

8. **"3× / CoStar Power Broker" strengthens the register row.** `content/stats.ts:43-46` → `StatsSection.tsx:80-84` renders `3×` as a Display-2 numeral over the label "CoStar Power Broker", with the quarters in `detail`. Ref 06's row is "CoStar Power Broker **Quarterly Deals**", and the source (`index.html:861`) used the same label but made the *quarters* its figure — the `3×` multiplier is ours. "3× CoStar Power Broker" reads as three wins of the annual Power Broker award rather than three Quarterly Deals placements; `content/team.ts:51` states it correctly.

9. **Two `PLACEHOLDER:` markers are unregistered.** `docs/PLACEHOLDERS.md` anchors 13 of the 14 markers in `app/privacy/page.tsx` (missing `:204`) and 4 of the 5 in `app/sms-terms/page.tsx` (missing `:156`) — both instances of the "VERBATIM PORT … Permitted legal-string substitutions: NONE" `PLACEHOLDER:counsel` banner. `app/accessibility/page.tsx` is 6/6. PHASE-1-EXECUTION §8.3 requires every marker registered as a DoD condition.

10. **No privacy notice at the calculator's point of collection.** `CalculatorResult.tsx:441-502` (`EmailCapture`) takes an email address and POSTs it to Web3Forms with no policy link, unlike the BOV form's consent-links row (`BovForm.tsx:526-546`). Already logged as an open counsel decision at `app/privacy/page.tsx:220`; no copy invented here.

11. **`content/doors.ts:50`** renders "A written number in 48 hours." unconditioned, which `content/methodology.ts:41` explicitly forbids ("may never be shortened to 'written BOV in 48 hours'"); the condition does render in the adjacent body at `doors.ts:51`, so the qualifier travels with the claim on-screen.

12. **FAQ placeholder notices render live** (`FaqSection.tsx:74-89`, six of them) — a deliberate, well-designed internal-preview state, but it must not survive the KW / Forward Wilshire gate.

13. **Observed mid-flight, flagged not diagnosed:** `CalculatorResult.tsx:283, 289` now render the two conditional caveats inside `<em className="not-italic …">`. The strings themselves are still the byte-exact frozen constants, but `index.html:1569-1570` set both in `<em>` and `content/compliance.ts:208, 213` annotate them as "italic". A concurrent agent was editing this file during the audit (`git status` showed it modified after my read), so this is reported as an inconsistency between the code and its own frozen-content annotation, not as a settled finding.

**Cleared under this gate (checked against the source, not assumed):** the two-sentence brokerage disclosure is byte-identical to `index.html:1140` and to ref 06 (codepoint diff, both sentences `EQUAL: true`) and renders on all four routes — `/` via `SiteFooter.tsx:152-158`, the three legal routes via `LegalPage.tsx:165-169`; there is no custom `not-found`/`error` boundary, so the route inventory is complete · the whole TCPA/10DLC block is byte-exact against `index.html:1174/1199/1200/1203`: `sms_consent_text`, the checkbox value, the full consent label, `labelSegments` concatenating back to `label` exactly, and the consent-links row — all five compared by codepoint, all `OK`; the registered brand string "Dino Monteverde (KW Commercial)" is identical across the checkbox, both legal routes and the 10DLC sample block · `buildBovPayload` (`lib/web3forms.ts:174-197`) submits `sms_consent_text` and an ISO-8601 `consent_timestamp` on **every** submit, adds `sms_consent` only when ticked, and the box ships unchecked and never `required` (`BovForm.tsx:195, 515-524`); both consent links open in a new tab with `rel="noopener"` · all four calculator disclaimers render byte-exact from `content/compliance.ts` — the methodology note (`index.html:920`) at `CalculatorSection.tsx:70`, `resultHonest`/`resultContext` plus both conditional caveats (`index.html:1566-1570`) at `CalculatorResult.tsx:275-289`, and the benchmark-band scope note from `index.html:1047` ("— broad national reference for this type, not your local comp set", no terminal period) at `CalculatorResult.tsx:309` · all 11 calculator ⓘ popovers verified verbatim against the source `data-tip` attributes (`index.html:937, 946, 950, 959, 974, 983, 992, 996, 1010, 1012, 1013`) — every prose run present, zero drift · every source fragment of `privacy.html` and `sms-terms.html` reproduced verbatim (automated fragment diff over both `<main class="legal">` blocks: zero missing), section numbering frozen, sections 10–12 appended not inserted · `/privacy` carries all five CalOPPA items and a CCPA/CPRA section, each with a visible `PLACEHOLDER:counsel` marker (`:210` PII categories, `:235`/`:286` third parties, `:271` update process + effective date, `:276` DNT/GPC, `:281` California Privacy Rights) and a conspicuous footer policy link on every route (`content/site.ts:247-251`) · Dino's name never appears in a broker capacity without a licence nearby — `TeamCard.tsx:144` renders `member.dre` (`CA DRE #01948432`) directly under his name and role, and both legal-page contact blocks sit above a footer carrying the full two-sentence disclosure · the consent modal genuinely refuses outside click: `ConsentModal.tsx:116-120` never `preventDefault`s, `ui/dialog.tsx:220-227` then `preventDefault`s and fires shake + `navigator.vibrate(50)`, and `@radix-ui/react-dismissable-layer@1.1.19/dist/index.mjs:57-58, 87-88` confirms a prevented `onInteractOutside` suppresses `onDismiss` · nothing `pending-verification` renders — zero Sarhan branding, zero "~$1B", zero prior-affiliation testimonials, and `JsonLd.tsx` deliberately emits no `award`, `aggregateRating` or `review` node (`:17-24`) and hangs the licence on the brokerage node, not on the unregistered team name · every other rendered figure traces to a `verified-current` row: `$200M+ / 12 (11+1) / 836K+` (`stats.ts`), all six closing prices and LP/SP-day pairs (`closings.ts:34-89`), the four reach stats (`methodology.ts:82-101`), the 180-day two-cycle framing, 60–90-day average close, and the four mandates · `#brands` carries `BRANDS_MICRO_LABEL = "[ FLAGS WE TRANSACT ACROSS ]"`, the `Marquee` label "Franchise flags we transact across" and `TRADEMARK_MICROCOPY` verbatim (`BrandsSection.tsx:87-103`) — the words "partners" and "clients" appear nowhere in any rendered string · KW mark alt text is byte-exact against `index.html:820` ("Keller Williams Commercial") · no performance guarantee and no unlicensed-activity verb in any rendered string, including the ten calculator ADVICE bodies, which are uniformly hedged ("typically", "often", "usually") · secrets clean: `FRED_API_KEY` is read only at `app/api/ticker-data/route.ts:172`, never `NEXT_PUBLIC_`, `git ls-files | grep -i env` returns nothing, and `git check-ignore` confirms `site/.env.local` and `site/.env.example` are both ignored by `site/.gitignore:34` · `INDEXING_ENABLED === false` (`lib/seo.ts:146`) so `robots.ts` disallows all crawlers and every route emits `noindex, nofollow`, holding the "no public deploy before the paperwork gate" guardrail.

**Verdict:** fail (two P0, neither inside this gate's editable scope — both escalated with exact remedies)

---

**Artifact:** THE PERFORMANCE GATE (ref 05 "Performance gates" + ref 07 P0 "Performance gate breach") across the whole landing route `/` — real landing-route JS weight measured from the shipped build, runtime-vs-import discipline for the 552KB city list and the 585KB ASCII grid, lazy-loading of Calendly and heavy widgets, CLS sources, `"use client"` boundary discipline, and rAF hygiene in `AsciiCanvas`.
**Date:** 2026-08-08

**Measured against build `tJn39eXejROwg59RPu1_e`, `site/.next/BUILD_ID` mtime 2026-08-08 12:04:57.** `pnpm build` is forbidden for agents (shared `.next`) and a sandboxed rebuild under a separate `distDir` was denied by the tool policy, so every number below is read off that build. Twenty source files under `site/components`, `site/content` and `site/lib` were modified by concurrent agents after 12:04:57 (`git status --porcelain`), so leaf-level byte counts may drift by a few hundred bytes. None of the structural findings depend on those files.

---

**P0 (blocker):**

**1. Landing-route JS is 270.2 KB gzip — 50% over the 180 KB budget.** Gate violated: ref 05 Performance gates, "JS shipped to client for the landing route ≤ 180KB gzip (fonts/images excluded)"; ref 07 P0, "landing JS >180KB gzip".

Method: the eleven `<script src>` chunks the prerendered `/` actually loads, taken from `.next/server/app/index.html` (identical to `.next/server/app/page/build-manifest.json` `rootMainFiles` plus the page's own chunks), each gzipped at level 9 and summed. `0cz1d0mv5g_q7.js` carries `noModule=""` in the HTML and is therefore excluded from the modern-browser total.

```
CHUNK                                 RAW       GZIP     BROTLI   ROLE
349j1nyctq4cj.js                     5598       2033       1711   framework
1-2tnky056fad.js                    34841       8765       7761   framework
03dw3rylmu6nw.js                   228841      71394      61123   react + react-dom
37952d0r4gvp6.js                   158502      43218      36744   next app-router client runtime
turbopack-2sxczevqfi5tk.js           9652       3867       3422   framework
43nv92kmyq4fu.js                    21311       5576       4907   layout-router + @vercel/analytics + speed-insights
3f8rblo1b5abt.js                    23645       8588       7638   next/link + dynamic loader
3li6aq8o9i2uu.js                   265046      89107      74829   ALL 14 app client entries (one chunk)
3lnny48_3-8n-.js                   132675      43750      38432   motion (full DOM feature set)
3trc0_k3_uja6.js                      488        372        292   app/template.tsx
0cz1d0mv5g_q7.js                   112594      39390      35158   legacy polyfills (noModule — excluded)
-----------------------------------------------------------------
TOTAL (incl. noModule polyfill)                316060     272017
TOTAL (modern browsers)                        276670     236859
BUDGET (ref 05) = 180KB gzip = 184320 bytes
OVER BY (gzip, modern) = 92350 bytes
```

Split: **framework floor 129,277 gzip** (react-dom 71,394 + Next app-router runtime 43,218 + 14,665 of small framework chunks) and **app code 147,393 gzip**. Brotli — what Vercel actually serves — is 236,859, still 52,539 over even if the gate were reinterpreted as brotli.

The structural point: the framework floor already consumes 70% of the budget. **The 180 KB number leaves ~55 KB gzip for all application code, and the app currently spends 147 KB.** No combination of the fixes below reaches 180 KB gzip while Radix, `motion` and a client-boundary route template all ship. This needs a dated PROJECT-MEMORY.md decision — either the budget is restated against a measured Next 16 + React 19 floor, or something structural comes out.

The four contributors, each measured by decoding the sourcemaps of the equivalent SSR chunks in `.next/server/chunks/ssr/` and attributing generated bytes back to source files:

**1a. The whole `motion` DOM feature bundle ships, and 39.8% of it is provably dead code.** `.next/server/chunks/ssr/node_modules__pnpm_0ee8qcf._.js` (the SSR twin of client chunk `3lnny48_3-8n-.js`) attributes as: `motion-dom` 97,773 · `framer-motion` 27,851 · `motion-utils` 2,178 · `lib/motion.ts` 1,215. Inside that, **layout projection 35,363 bytes** (`motion-dom/src/projection/**`, of which `create-projection-node.ts` alone is 24,350 = 18.9% of the chunk), **drag/pan/inertia 14,563 bytes** (`framer-motion/src/gestures/drag/VisualElementDragControls.ts` 7,344, `gestures/pan/PanSession.ts` 3,943, …), and `framer-motion/src/motion/features/layout/MeasureLayout.tsx` 1,474 — **51,400 of 129,088 attributed bytes.** Nothing on this site uses any of it: `grep -rn "layout=|layoutId|drag|whileDrag" components app --include=*.tsx` returns only `draggable={false}` on five `<img>`s and one prose sentence in `app/accessibility/page.tsx:99`.

Cause: four files import the full `motion` proxy, which binds the `domMax` feature set (`framer-motion/dist/es/render/dom/features-max.mjs` = `{...domAnimation, ...drag, ...layout}`):
- `site/app/template.tsx:32` — `import { motion, useReducedMotion } from "motion/react"`
- `site/components/motion/Reveal.tsx:33-38` — `motion[as]` at `:198` and `:220`
- `site/components/atoms/Stamp.tsx:42`
- `site/components/ui/dialog.tsx:30`

Remedy, verified feasible against `node_modules` (not against a build): `domAnimation` (`features-animation.mjs`) = `{renderer, ...animations, ...gestureAnimations}`, and `gestures.mjs` exports `inView`, `tap`, `focus`, `hover` while `animations.mjs` exports `animation` and `exit` — so `domAnimation` covers **every** motion feature this site uses (`variants`, `animate`, `whileInView`, `AnimatePresence`/`exit`, hover/tap/focus). Swap those four imports to `import * as m from "motion/react-m"` and wrap the tree once in `<LazyMotion features={domAnimation}>` inside `app/template.tsx` — template wraps every route's children, including everything `app/page.tsx` renders, and React context crosses Radix's `createPortal`, so one provider covers the consent modal and the menu overlay too. **Estimated saving ~17 KB gzip** (39.8% of the 43,750-gzip chunk; derived from sourcemap byte attribution of the SSR twin, *not* directly measured — I could not rebuild).

**1b. There is no code splitting at all.** `grep -rn "next/dynamic|await import\(" app components lib` returns exactly one hit in the entire application: `site/components/motion/SmoothScroll.tsx:58` (`import("lenis")`). Consequently all fourteen client entry points that `/` reaches — enumerated from `.next/server/app/page_client-reference-manifest.js`: `app/template.tsx`, `art/AsciiCanvas`, `atoms/PhotoFrame`, `atoms/Stamp`, `calculator/Calculator`, `forms/BovForm`, `modals/ConsentProvider`, `motion/CopyButton`, `motion/CountUp`, `motion/Reveal`, `motion/SmoothScroll`, `sections/SiteNav`, `ticker/TickerClient`, `ui/accordion` — and their whole transitive graph compile into **one 265,046-byte (89,107 gzip) render-blocking chunk**. The valuation calculator (section 03), the BOV form (section 09) and the FAQ accordion (section 08) are all in the critical path of the hero.

**1c. Radix + its floating/scroll-lock satellites are 40% of that chunk and almost all of it is below the fold.** From `_0ob1owo._.js.map`: `@radix-ui` **74,788 (28.6%)** · `@floating-ui` **19,598 (7.5%)** · `react-remove-scroll` 5,338 · `react-remove-scroll-bar` 1,700 · `aria-hidden` 1,479 · `use-sidecar` 1,073 — **103,976 raw bytes.** `@floating-ui` enters only through `ui/popover.tsx` (used by `calculator/InfoPopover.tsx`) and `ui/select.tsx` (used by `calculator/CalculatorSteps.tsx`), i.e. the third section down, behind a click. Only `ui/dialog` genuinely earns first-load status, because `ConsentProvider` opens the consent modal for every first-time visitor (`ConsentProvider.tsx:79` `setOpen(stored === null)`).

**1d. `tailwind-merge` is 26,872 raw bytes (10.3%) of the app chunk** — `tailwind-merge/src/lib/default-config.ts` alone is 21,040 — shipped so that `cn()` in `site/lib/utils.ts` can resolve Tailwind class conflicts at runtime. For a token-only design system where the conflict cases are known at authoring time this is the worst bytes-per-value line in the bundle. (`CalculatorResult.tsx:388-390` already documents authors routing around `cn()` precisely because twMerge collapses `text-body` and `text-fg-muted` into one group.) Not fixed: `lib/` is fenced and dropping twMerge changes override semantics in ~70 call sites.

---

**P1 (hierarchy/spacing/consistency/motion discipline):**

**1. `will-change` is set permanently on 15 elements of the landing route, 12 of them images.** `site/app/globals.css:588` puts `will-change: filter` in the `photo-reveal` utility itself, so every photo the page renders holds a promoted compositor layer and a filter-ready buffer for the whole session, whether or not it is ever hovered — and on touch, where the hover rule at `:589-594` can never match, it is pure cost. Evidence: `grep -o "photo-reveal" .next/server/app/index.html | wc -l` → **12**. Plus `site/components/motion/Marquee.tsx:98` sets `will-change-transform` on both marquee tracks unconditionally, so it survives `prefers-reduced-motion` and `:root[data-motion="off"]` where the animation itself is stopped (`grep -o "will-change-transform" .next/server/app/index.html | wc -l` → 3). Remedy: move `will-change` onto the hover/animating state, not the resting state. Not fixed — `globals.css` and `components/motion/` are both outside this task's write scope.

**2. The nav reflows its own row on scroll — a CLS source that is not input-excluded.** `site/components/sections/SiteNav.tsx:309-310` swaps the active link between `font-semibold` and `font-normal`. Weight changes advance width, so each time the active-section IntersectionObserver fires, the active `<li>` grows and pushes every sibling in the `gap-8` flex row. Scrolling does not set `hadRecentInput` on a layout-shift entry (that flag covers discrete input — pointerdown, keydown), so these shifts *are* counted against CLS. The area is small, but it fires once per section boundary for the whole page. Remedy: reserve the bold width (a `::after` ghost with the same text at `font-semibold`, or `font-variation-settings`/`text-shadow`-based faux-bold), or carry "active" on the existing `border-b-2` and colour only — the border already switches `border-transparent` → `border-accent-text` at zero layout cost.

**3. `TickerBar` is mounted inside the transformed route template, which its own header forbids.** `site/app/page.tsx:67` renders `<TickerBar />` inside the page tree, therefore inside `app/template.tsx`'s `motion.div`. `site/components/ticker/TickerBar.tsx:13-16` states the contract explicitly: "Last element inside `<body>` in `app/layout.tsx` — after the footer, OUTSIDE `app/template.tsx`. Not negotiable" — because a transformed ancestor becomes the containing block for a `position: fixed` descendant. `template.tsx` animates `y: 8 → 0` over `DUR.base` on every client-side navigation (`lib/motion.ts:79-86`), so for 300ms on each footer link to `/privacy`, `/accessibility` or `/sms-terms` the fixed rate bar detaches from the viewport and rides the page. Not fixed — `app/page.tsx` and `app/layout.tsx` are outside this task's write scope. The fix is to move line 67 of `page.tsx` into `app/layout.tsx` after `{children}`.

**4. The ASCII loop asset is 206 KB gzip and its fetch opens in parallel with the render-blocking JS.** `public/art/ascii-gold.json` is 588,567 bytes raw / **205,965 gzip** (blue: 584,242 / 205,364). `AsciiCanvas.tsx:1013-1029` starts `begin()` from an `IntersectionObserver` at `threshold: 0`, and the canvas is the hero — so on every fine-pointer, motion-allowed load the fetch begins immediately, competing with 270 KB of blocking script for the first RTTs. It is under ref 05's own "loop file/frames ≤1.5MB" ceiling and it is correctly gated off for touch, reduced-motion and data-saver, so this is not a P0; but it is the single largest first-load transfer on the site and it lands during hydration. Worth deferring behind `requestIdleCallback` or the `load` event, or shipping the 28 luminance planes as a binary payload (the decode at `AsciiCanvas.tsx:336-352` already reduces them to one `Uint8Array` per frame — 10,240 bytes × 28 = 286,720 bytes uncompressed, which would compress far better as bytes than as base-36 text).

**5. Lenis loads on mount, not on interaction.** `site/components/motion/SmoothScroll.tsx:73-77` calls `sync()` → `start()` → `import("lenis")` from the mount effect, so every fine-pointer visitor fetches the 18,703-byte chunk (`3uz1v362glmsh.js`) on load. Correctly split and correctly gated on `motionAllowed()` and pointer type, so it is not blocking — but ref 05's wording is "lazy-load on interaction only", and smooth scroll is not needed until the first scroll. One `{ once: true }` scroll or wheel listener would move it off the load path entirely.

---

**P2 (polish):**

1. `site/components/nav/MenuOverlay.tsx:64` imports the entire `closings` array (1,765 bytes minified, in the client chunk) to `find()` one property at `:102` for a decorative overlay photo. Every closing's price, keys, cap rate and date ships to the client for an `src` and an `alt`. Passing the photo down as a prop would drop it, but the only server ancestor is `app/page.tsx` (fenced), so this needs an owner.
2. `AsciiCanvas`'s rAF is scheduled on every frame while the canvas is in view, and `tick()` early-returns at `:943` when nothing is due. With `fps: 4` in the shipped asset an ambient step lands every 250ms, so roughly 14 of every 15 ticks are pure no-ops. Cheap, but it is a wake-up every 16ms for the life of the hero; a `setTimeout`-scheduled resume when the pointer is out and no shimmer is live would idle it properly.
3. `app/layout.tsx:82-83` mounts `@vercel/analytics` and `@vercel/speed-insights`, which each inject a further runtime script from `/_vercel/*` on mount. They are 5,576 gzip inside the bundle (shared with `layout-router`), but the two injected scripts are additional first-load network requests that the 180 KB accounting above does not include.
4. `site/public/brand/lockup-stacked-gold.png` is 278,213 bytes as a source asset for a mark rendered at 40–48px (`SiteFooter.tsx:108-114`, `width={240} height={184}`). `next/image` resizes it, so no client cost — but it is 278 KB in the repo and in the deploy for a 240px raster.

---

**Gates that PASS, with the evidence:**

- **Gate 2 — the big JSON assets are fetched, never imported.** `grep -rn "from ['\"].*\.json['\"]|import\(['\"].*\.json" app components lib content` → no matches. `us-cities.min.json` is reached only through `fetch(DATA_URL)` at `CityPicker.tsx:88`, behind `ensureData()` which fires on field focus (`:363-364`) and on retry (`:269`), never at import. The ASCII grid is reached only through `fetch(ASCII_ASSETS[palette].json)` at `AsciiCanvas.tsx:991`, inside `begin()`, which only runs on first intersection. Confirmed in the shipped output: `grep -rlE '"lum"|"seamRow"|"charAspect"' .next/static/chunks/*.js` → no match; `grep -rl "Abbeville\|Aberdeen" .next/static/chunks/*.js` → no match. (Also checked while in there: `grep -rl "FRED_API_KEY" .next/static` → absent.)
- **Gate 3 — Calendly loads on interaction only, and right now not at all.** `CalculatorResult.tsx:175-199` builds the `assets.calendly.com` `<link>`/`<script>` inside `loadCalendly()`, which is called only from the `open()` click handler at `:406`; the module-level `calendlyLoad` promise memoises it. `CALENDLY_URL` is null (`content/site.ts:156-157`, `blocked: calendly-url`), so `:396` returns a plain `<a href="#bov">` and nothing is requested from Calendly at all. No `intl-tel-input` in the tree (`PhoneField.tsx` is hand-rolled; the package is not in `package.json`).
- **Gate 4 — CLS reservations hold, apart from P1-2 above.** Every `next/image` is dimensioned: `PhotoFrame.tsx:96` makes it a TypeScript either/or (`aspect` + `sizes`, or `width` + `height`), `SiteFooter.tsx:111-112` and `:145-146` and `LegalPage.tsx:159-160` pass explicit pairs. The three plain `<img>`s all carry intrinsic attributes — `AsciiStatic.tsx:88-89` (960×640, the real `viewBox`), `Stamp.tsx:90-91`, `Wordmark.tsx:74-75`. The ticker reserves its own height from first paint via a flow spacer built from the same class string as the bar (`TickerBar.tsx:50-57`) and each value sits in a `min-w-[6ch]` mono slot (`TickerClient.tsx:112`), with only `X.XX%`-shaped values admitted. The ASCII box is reserved by the server-rendered static SVG that sits underneath the canvas (`AsciiCanvas.tsx:1100-1107`). Marquees are fully server-rendered including the clone (`Marquee.tsx:91-96`), so they are at final width on first paint. The calculator's result area is not "reserved", but every step change is user-initiated and therefore CLS-exempt, and only one step is ever mounted (`Calculator.tsx:109`). Reveals never hide anything above the fold — `Reveal.tsx:204` ships `initial={false}` and `:161` refuses to arm any element whose top is inside the viewport, so JS-off and hydration-failure both render at full opacity. `CountUp.tsx:120` renders the final string server-side. Fonts carry `size-adjust` fallback metrics (`115.45%` Fraunces / `107.12%` Inter / `134.59%` IBM Plex Mono in `.next/static/chunks/35177gddqthwl.css`) with `display: swap` and five preloaded woff2 files, ≤2 per family.
- **Gate 5 — client-boundary discipline is good; 26 of 27 boundaries earn their keep.** The real directive count is 27, not the 30 a naive `grep -rl '"use client"'` returns: `components/hero/HeroCoverPanel.tsx`, `components/sections/FaqSection.tsx`, `components/sections/MandatesSection.tsx` and `components/ui/button.tsx` match only in prose and are Server Components. Every section under `components/sections/` except `SiteNav` is a Server Component, and `Marquee`, `AsciiStatic`, `CardShell`, the cards, `SectionHeader` and the rest of `atoms/` stay server — the boundary is genuinely pushed down into islands, which is what the brief asks for. Each remaining boundary was checked against what it actually does: canvas/rAF (`AsciiCanvas`), `useState` + DOM event (`PhotoFrame`, `Stamp`), Radix (`accordion`, `checkbox`, `dialog`, `popover`, `select`), form state (`Calculator` + 4, `BovForm`, `CityPicker`, `PhoneField`), storage/context (`ConsentProvider`, `ConsentModal`), IntersectionObserver (`SiteNav`, `Reveal`, `CountUp`), fetch (`TickerClient`), clipboard (`CopyButton`), Lenis (`SmoothScroll`), overlay state (`MenuOverlay`). The one I would challenge is **`app/template.tsx`**: it exists solely to fade client-side navigations between four routes, it deliberately does nothing on first paint (`:41-49`), and it costs a client boundary at the root of every route plus the transform that breaks `position: fixed` in P1-3. Deleting it would remove `3trc0_k3_uja6.js` and one of the four full-`motion` importers.
- **Gate 6 — `AsciiCanvas` rAF hygiene is clean, and materially better than the spec asks.** In-viewport gating: `sync()` at `:974-985` starts the loop only when `engine !== null && inView && !frozen`, and `:1013-1029` drives `inView` from an `IntersectionObserver`; off-screen the loop is `cancelAnimationFrame`d outright, and a pointer that was inside is converted to a `pendingLeave` so the shimmer cannot survive the scroll-out. Work gating: `:941-943` returns before touching the clock unless an ambient frame is due, the pointer/shimmer is live, or the dirty ring is non-empty. Zero allocations in the loop: every buffer is preallocated in `createEngine` (`:469-485`), the glyph table and every colour string are built once in `decode`/`buildPalette`, `resolve` writes to module-scope scratch (`outGlyph`/`outColor`) instead of returning an object, `computeBounds` writes to `b0r/b1r/b0c/b1c`, the dirty ring is compacted with `copyWithin` (`:775`), and the one `measureText` (`TextMetrics` is an allocation) is confined to `applyResize` (`:622-628`) which runs on resize only. Dirty-rect redraws: `scan`/`drain` (`:667-779`) merge queued cells into per-row runs and clear only those runs; full repaints happen on mount, resize and webfont swap only (`:786-796`, `:933-939`), and are deliberately outside the kill-switch accounting. DPR cap: `Math.min(window.devicePixelRatio || 1, HERO_BUDGET.maxDpr)` at `:601` with `maxDpr: 2` at `lib/motion.ts:118`. Kill switch: `:966-971` compares `performance.now() - t0` against `HERO_BUDGET.killFrameMs` (12) and calls `kill()` after `killConsecutiveFrames` (30) — `lib/motion.ts:122-123` — and `kill()` at `:903-920` freezes to the static frame, sets a module-scoped `sessionFrozen` so a remount cannot re-arm it, and calls `freezeMotion()` to stop the rest of the site too. The 4ms drain budget (`HERO_BUDGET.frameBudgetMs`) is applied at `:964` with the clock read every 256 cells (`DRAIN_CHECK_CELLS`) rather than per cell. Shipped asset: 160×64, 26 levels, 28 frames at `fps: 4` — a 7s loop, inside ref 05's 24–36 frames / ≤12s / ≤24fps envelope.

**Not verified (and not verifiable from this workstation):** LCP, INP, the CLS *score*, Lighthouse mobile 90/95, and 60fps under 4× CPU throttle all require a running page — `pnpm dev`/`pnpm build` are forbidden for agents and a sandboxed rebuild under a separate `distDir` was denied. Everything above is static analysis of the 12:04:57 build plus source reading. The ~17 KB gzip saving quoted for the `motion` fix is a sourcemap-derived estimate, not a measurement.

**No code was changed by this audit.** Every fix identified lands in a file this task may not write: `app/globals.css`, `app/layout.tsx`, `app/page.tsx`, or a file under the `lib/` · `content/` · `ui/` · `atoms/` · `motion/` · `cards/` fences that `docs/AGENT-BRIEF.md` closes to non-owners. Concurrent agents were actively editing twenty of those files during this audit (`site/lib/valuation.ts` at 12:17:55, `site/components/sections/BrandsSection.tsx` at 12:16:52), and with no ability to rebuild there is no way to verify a refactor of this size would not regress. Reporting with exact call sites is the correct trade here; the remedies above are written to be executable as-is by each file's owner.

**Verdict:** fail — one P0 (landing-route JS 270.2 KB gzip against a 180 KB gate, +92,350 bytes), which additionally exposes that the budget itself is unreachable given a 129 KB gzip Next 16 + React 19 floor and needs a dated PROJECT-MEMORY.md decision.

---

**Artifact:** THE CALCULATOR PARITY GATE — `site/lib/valuation.ts` + `site/lib/valuation.test.ts` against the authority `~/Documents/Dino/dino-sites/kwc-dinomonteverde/index.html` (calculator IIFE `:1355-1687`), plus the wizard-UI field/option/popover/result inventory against port pack §B.
**Date:** 2026-08-08

**Method (so the "pass" is falsifiable):** the source IIFE was transcribed *literally* into an independent reference model (`var`, operator order, DOM-value inputs) with no reference to the port, then both were run side by side and compared on 38 fields per case — 12 raw floats by exact IEEE identity, plus every rendered string, bar percent, fired-code list, CTA and prefill key. **109,200 cases** (5 types × 4 tiers × 3 conditions × 2 land × 2 brand × 7 F&B values incl. the 25% boundary and 25.000001 × 5 NOI values × 13 keys/occ/ADR tuples) = **4,149,600 assertions: zero divergences.** Separately, the ADVICE array, fallback, three CTA lines, CONFIG, TYPICAL, the three benchmark bands and the two clamp literals were re-extracted **from the source file's raw bytes** and byte-compared: zero divergences. The live input formatters (`num`/`clean`/`formatField`/blur) were fuzzed over **40,056 inputs × 13 assertions = 520,728**: zero divergences.

**P0 (blocker):** **none.** Every constant, adjuster, guard, control-flow ordering and rounding step matches the source. Specifically cleared: all 20 cap-rate cells; `roomsToTotal`; `noiMargin`; all four adjuster tables; `fbThreshold 0.25` with `>` not `>=`; the `0.045` floor and `capLow + 0.005` spread (floor applied *before* the spread reads it); all 45 OCC/ADR/REVPAR band values; all four TYPICAL rows; and all nine ADVICE rules — id, priority, predicate and body byte-identical, declaration order preserved as the priority tie-break. **The trap named in the brief is not present:** `index.html:1494` tests `c.brand === "indep"` and `valuation.ts:727` tests `c.brand === "indep"`; the ctx assignment at `valuation.ts:912` mirrors `:1519`. The rule is live, not dead — it fires in the 109,200-case sweep and in three existing tests. Control flow verified line-for-line: rounding *follows* the per-key division (`:1553-1554`), the $50K total rounding and the $1K→$5K per-key double-rounding (D6) are both preserved, `valueHigh = noi / capLow` / `valueLow = noi / capHigh` are not transposed, `noiPerKey` is guarded on `keys > 0` while the per-key divide is not (D1/D2, locked), and `ctaLine` is computed from `firedCodes` *before* the fallback is substituted while `prefill.topAdvice` is read *after* it.

**Hand-computed scenarios (source arithmetic by hand → ported engine, compared to the dollar):**
| # | Scenario | By hand from source | Ported engine |
|---|---|---|---|
| S1 | **demo script — 120-key select-service suburban**, 72% / $165, branded, 4–8 yrs, fee simple | rooms 120×165×365×0.72 = 5,203,440; ÷0.88 = 5,913,000; ×0.34 = **NOI 2,010,420**; cap 8.00−0.25 = 7.75% / 9.50−0.25 = 9.25%; value 21,734,270.27 – 25,940,903.23; totals **21,750,000 / 25,950,000**; per key 181,000 / 216,000 → **$180K – $215K**; RevPAR 118.8 → $119; NOI/key 16,753.5 → $16,754; codes `[]` | **identical to the dollar** — `$21.8M – $26.0M`, `$180K – $215K`, `7.8% – 9.3%`, `$119`, `$16,754`, `[]`, default CTA, prefill `$21,750,000 – $25,950,000` |
| S2 | seeded on-load defaults (Full-Service, suburban, 88 / 74% / $198) | NOI 2,027,295.8030769234; totals 22,550,000 / 26,150,000 | identical — `$22.6M – $26.2M`, `$255K – $295K`, `7.8% – 9.0%` |
| S3 | independent limited-service, tertiary, **ground lease + PIP due**, 54 keys | adj +75+100+25 = +200bps → 11.25% / 13.00%; NOI 360,771.84; totals 2,800,000 / 3,200,000 | identical — codes `[pip, ground, revparLow, valueAdd, independent, smallKeys]`, value-add CTA |
| S4 | gateway full-service, **F&B 32% + NOI override $9.25M**, 310 keys | override bypasses the revenue model; adj −50+0−25+25 = −50bps → 5.75% / 7.00%; totals 132,150,000 / 160,850,000 | identical — `$29,839*` (asterisk + NOI caveat), codes `[revparTop, bigKeys]`, runningWell CTA |
| S5 | resort/boutique secondary, soft-brand→branded, **autofilled typicals** | NOI 1,958,283.8709677416; totals 23,750,000 / 29,000,000 | identical — `usedDefaults` caveat present, codes `[revparLow]` |

**P1 (hierarchy/spacing → here, regression exposure):** **FIXED, both inside scope.**
1. **`valuation.ts:905` held a dead `const fbPct = (input.fbPct ?? 0) / 100` that was assigned and never read**, sitting five lines above `:935`, which correctly passes `input.fbPct` as a *percent* into `capAdjustment` (which does the `/100` itself). `noUnusedLocals` is off in `site/tsconfig.json`, so tsc never flagged it. A future "consistency fix" wiring the local in would have divided by 100 twice, pushing `fbThreshold` out to 2500% and silently killing the +25bps F&B adjuster in a *frozen* file. Replaced with an explicit unit-contract comment; behaviour provably unchanged (the 109,200-case sweep re-run clean afterwards).
2. **Parity holes in the suite — 54 exports had zero test references**, including the ones that exist *solely* as parity guards. Proven non-vacuous by mutation testing: three real parity breaks **passed silently under the old 103-test suite** and are now caught — removing `FIELD_FORMATS.pct.max` (which is occupancy clamp layer 1; the old tests inlined `{dec:1,max:100}` literals instead of asserting the shipped map), swapping `PROPERTY_TYPE_OPTIONS[2]`/`[3]` (which silently changes `INITIAL_FORM`'s seeded default *and* `showsFoodAndBeverage`, both of which index into that array), and downgrading the `formatOccBandSub` en dash U+2013 to ASCII. Added 17 tests (103 → **120, all passing**) covering: all five shipped `<option>` lists byte-exact in source order (U+2026 ellipsis, U+2013 dashes) with the seeded-default indexes pinned; the `shim(label) === value` round-trip for all 18 labels (the invariant that makes the components' raw-display-string state shape safe); the two collapsing labels; the `*_LABEL` prefill maps; `FIELD_FORMATS` vs the source `fmtMap`; the `*_BY_LABEL` D4 re-keying guard cross-checked against the enum tables and through `calculate()`; `blurNumericField` and `cleanNumericInput` (both previously untested and both wired into every numeric field); the two bar sub-label formatters; `RESULT_DISCLAIMER` / `KEYS_REQUIRED_ERROR` / `TYPICAL_FIGURES_NOTE` / `VALUE_ADD_CTA_CODES`; the five named increments; and a direct regression guard that `capAdjustment` takes F&B as a percent. Five further mutations (`"indep"`→`"independent"`, the F&B double-divide, a transposed cap-rate digit) were all caught. Working tree restored after every mutation.

**P2 (polish) — escalated, outside this gate's editable files, no edit made:**
3. **`site/components/sections/CalculatorSection.tsx:70` and `site/components/calculator/CalculatorResult.tsx:278`** both render "verified comps backed by **CoStar and RCA**". Port pack §B.5 explicitly flags this string for the evidence gate, and **"RCA" appears nowhere in ref 06's claims register** (`grep -rn "RCA" .agents/skills/hokuten-design-director/references/` → no match; CoStar alone has `verified-current` rows at `06-content-and-proof.md:30, 32, 36`). Needs a register row or a copy correction before public deploy. Not a parity defect — the string is byte-exact to `index.html:920, :1568`.
4. `site/components/calculator/BenchmarkBars.tsx:73` uses `Math.round(row.pct)` where the source used `pct.toFixed(0)` (`:1616`). **Verified equivalent, not a finding** — proven identical over 1,000,001 sampled values across the whole reachable [0,100] range; recorded so it is not re-flagged.

**UI inventory vs port pack §B — complete, nothing dropped.** Step 1: Property Type, Keys, Where is it?, Brand, ZIP (optional, `maxlength=5`, `autocomplete="postal-code"`, no ⓘ — matching the source), and the "Refine my estimate (optional)" disclosure with Condition, Land, F&B (conditional) and NOI. Step 2: the TTM note with its inline ⓘ, Occupancy, ADR, live RevPAR preview, the typical-figures autofill link and its note, Back, Calculate. Step 3: value range, `resHonest`, `resContext` with both conditional caveats, all four "How we got there" chips **in source order with the exact glosses**, both benchmark bars with their sub-labels, one-or-two advice paragraphs plus exactly one CTA line, "What happens next" with its intro and all four items, the primary CTA, the email capture, the Calendly CTA and "Start over". All **11** source ⓘ triggers present and attached to the right fields. `showsFoodAndBeverage` reproduces the source's strict two-label equality (`:1670`). All three occupancy-clamp layers (§B.7) present and wired. The one intentional copy deviation — `CalculatorSection.tsx:43` dropping "from comp data" — is spec decision C1 and is *correct*: the model is explicitly generalized assumptions (`index.html:1352-1353`), so shipping the claim would contradict the methodology note four lines below it.

**Not verified (stated so no one infers coverage I did not provide):** no browser render, so zero visual/interaction/375px QA; I checked the ⓘ *inventory and attachment points*, not the tip prose against the source `data-tip` attributes; `lib/web3forms.ts` transport is untouched; and `CalculatorResult.tsx` was modified by a concurrent agent after my read, so my inventory of that file reflects its pre-edit state.

**Commands run:** `npx vitest run` → `Test Files 1 passed (1) · Tests 120 passed (120)`. `npx tsc --noEmit --incremental false` → exit 0, **zero output lines** (whole project clean).

**Verdict:** pass — the port is bit-exact to the authority; two P1 regression-exposure findings fixed in scope, two items escalated.
